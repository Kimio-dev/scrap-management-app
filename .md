prisma studio起動 node node_modules/prisma/build/index.js studio
[ Webアプリ (React / Express) ] ───┐
                                  │
[ Prisma Studio (ブラウザ画面) ] ──┼─→ 【 共通の PostgreSQL データベース 】
                                  │
[ pgAdmin (管理ツール) ] ──────────┘

validation
 frontend 
  src/pages/list.tsx
   ・そもそも月締めされている月のデータは、ユーザーに新規登録も編集もさせないようにボタンをロックする。
   ・未来日付はグレーアウトして押せないようにする。

  src/component/ScrapCreateModal.tsx
   ・回収日、回収量に対してのrequired
   古いブラウザ、開発者ツールでrequiredを消してデータ送信してもブロック
   ・保存ボタン2連打防止。1回押した後、通信が完了するまで、ボタンを非活性に。
   ・全角入力を自動で半角に変換
   ・バックエンドエラーメッセージをフロントに出す
   

  ScrapEditModal.tsx
   ・回収日、回収量に対してのrequired
   ・古いブラウザ、開発者ツールでrequiredを消してデータ送信してもブロック
   ・保存ボタン2連打防止。1回押した後、通信が完了するまで、ボタンを非活性に。
   ・削除ボタン実行後は、確認ボタン出す。
   ・全角入力を自動で半角に変換
   ・バックエンドエラーメッセージをフロントに出す
 
 backend
  middlweares-----------------
   ・月締めチェック        
   ・未来日チェック       
   ・回収量minチェック　　
   ・担当者のIDチェック
   ・空白、null、undefindチェック
  scrapRouters.ts
   middlwearesをルーティングに組み込み

メモ
次やること
・usersテーブルはログテーブルのUI構築の要否
・月締め処理登録UIの要否
・rechartで月別スクラップ発生量集計

本番環境で構築すること
・認証と認可(ユーザーIDとパスワードで)
・権限別画面構成(admin,スクラップのcrud,read_only)